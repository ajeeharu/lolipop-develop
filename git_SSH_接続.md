# SSH接続
- LolipopとのSSH接続（公開鍵・秘密鍵）

## 事前準備：Lolipop（レンタルサーバー）のSSH設定

- **SSHの状態**：有効にする 

下記の項目をメモしておく
1. **サーバー**
1. **アカウント** 
1. **接続ポート** 
1. **SSHパスワード** 


## 鍵ペア（公開鍵・秘密鍵）の作成
### Ubuntuのターミナルで以下のコマンドを入力

```bash
ssh-keygen -t rsa -b 4096
```
- 保存先: そのままEnter（~/.ssh/id_rsa に保存）
- パスフレーズ: 鍵を使用する際のパスワードです。任意ですが設定を推奨します。
### 秘密鍵・公開鍵
#### id_rsa （秘密鍵：誰にも渡さない！）
#### id_rsa.pub （公開鍵：サーバーに渡すもの）

## サーバーへ公開鍵を登録する
### Ubuntuのターミナルで以下のコマンドを入力
```bash
ssh-copy-id -p [接続ポート] [アカウント]@[サーバー]
# ※パスワードを求められるので、ロリポップのSSHパスワードを入力
```

## 秘密鍵で接続テスト
### Ubuntuのターミナルで以下のコマンドを入力
```bash
ssh -i ~/.ssh/id_rsa -p [ポート番号] [アカウント]@[サーバー名]
# 接続ができることを確認
```

## 接続の簡略化
### PCに　~/.ssh/config　を以下の内容で作成
```bash
# ~/.ssh/config
Host lolipop
    HostName [サーバー]
    User [アカウント]
    Port [接続ポート]
    IdentityFile ~/.ssh/id_rsa
```
これ以降は下記のコマンドで接続

```bash
ssh lolipop
```

# GitHub Actionsの設定